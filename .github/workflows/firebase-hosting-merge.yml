# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Run script
        run: |
          # Set your Firebase project ID and custom domain
          project_id="your-firebase-project-id"
          domain="ude-d8926"

          # Authenticate with Firebase CLI
          npm install -g firebase-tools
          firebase login

          # Set the Firebase project
          firebase use "$project_id"

          # Set the custom domain for hosting
          firebase target:apply hosting "$domain" "$domain"

          # Deploy the project to the custom domain
          firebase deploy --only hosting:"$domain"

